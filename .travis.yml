sudo : enabled
language : java

before_script : apt-get install jflex && make
after_script : make fclean

matrix:
  include:
    - os: linux
      env:
         - NAME="Single Compile test"
    - os: linux
      script : make test
      env :
         - NAME="Full Build All"
