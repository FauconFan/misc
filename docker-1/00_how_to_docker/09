apt-get update
apt-get install -y --no-install-recommends \
		gcc \
		git
apt-get clean
rm -rf /var/lib/apt/lists/*

# Pour tester ces commandes:
# docker-machine scp 09 Char:/home/docker/09
# docker run --rm -it -v /home/docker:/42 debian
# cd /42
# ./09
